# pandas-sqlachemy

Este código é usado para pegar dados de um arquivo CSV e inserí-los numa base de dados. Para tanto, deve-se ter a base de dados já criada. A tabela é criada pelo código, usando a biblioteca sqlalchemy. O arquivo .csv é lido através da biblioteca pandas.

